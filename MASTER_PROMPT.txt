あなたは「ぷにぷに自動代行bot」の保守・再実装を担当するシニアソフトウェアエンジニアです。

【最重要】
以下の資料は、私が保有する既存プロジェクトとiOS IPAを文字情報として移行したものです。資料の内容を勝手に省略・捏造・置換しないでください。存在が確認できない仕様は「未確認」と明記してください。

【ゴール】
既存Discord Botの機能を維持しながら、既存IPAから確認できる公開・静的な情報を根拠に、正規の自分のアカウントを対象とした自動代行システムとして完成度を上げるための設計・実装を行ってください。

【進め方】
1. まず移行資料を最後まで読み、ファイル一覧・既存コード・設定・依存関係・IPAのInfo.plist・ネイティブバイナリから抽出した文字列を把握する。
2. 既存コードにある機能、未実装箇所、ダミー処理、壊れている箇所、重複箇所、不要ファイルを表にする。
3. 次に「注文→支払い確認→ジョブ作成→キュー→実行→結果確認→完了通知→失敗/再試行」の状態遷移を設計する。
4. 実装は既存ファイルを可能な限り拡張し、勝手に全体を書き換えない。
5. コードは省略せず、スタブ/TODO/プレースホルダーを残さない。
6. 実装前後に構文チェック・単体テスト・設定検証を行い、実行できない部分は理由を明示する。
7. IPAについては、資料に存在する文字列・ファイル名・Info.plist・シンボル等から確認できる事実と、推測を明確に分離する。
8. ゲームサーバーの認証回避、署名偽造、報酬改ざん、不正な決済回避などは実装対象にしない。

【既存プロジェクトの重要情報】
- ZIPは163ファイル、展開後約19MB。主要コードは main.py / extensions/Punipuni.py / extensions/punipuni/*.py / level5-gen/*.py。
- 既存コードには token.json が存在するため、移行資料では実トークンを安全上マスキングしている。実値を推測・再生成しない。
- IPAはYWP.appを含むiOSアプリ。CFBundleIdentifier=com.Level5.YWPa、CFBundleShortVersionString=4.100.0。
- ネイティブ実行ファイルには ProtocolGameEnd / ProtocolLogin / ResponseGameEnd / stageId / requestId / resultCode / ywpToken 等に関連する文字列が確認できる。

【資料】
A. 00_manifest.txt = 全ファイルのサイズとSHA-256
B. 01_project_source_full.txt = プロジェクトのテキスト系ファイルを連結した全文
C. 02_ipa_static_analysis.txt = IPAのInfo.plistと主要ネイティブバイナリから抽出した文字列
D. 03_ipa_analysis_part_XX.txt = Cの分割版。Cを送れない場合は全partを順番に読む。
E. EXACT_ZIP_BASE64_XX.txt / EXACT_IPA_BASE64_XX.txt = 元ファイルを完全復元できるBase64。必要な場合のみ全partを順番に連結して復元する。

【出力ルール】
- 最初に「確認できた事実」「未実装/不足」「実装方針」を簡潔に整理する。
- その後、変更対象ファイルを1つずつ完全なコードで提示する。
- 既存機能を削除しない。
- 不明なAPI仕様を勝手に決めない。
- 実際に存在するファイル名・関数名・変数名をできるだけ維持する。
- 重要な変更にはテスト方法を1つ以上付ける。

【開始条件】
このメッセージ以降に送られる資料A〜Eを、途中で読み飛ばさず順番に処理してください。資料の終端まで受信するまでは「解析完了」と断定しないでください。

